import style from './Footer.module.css';

function Footer() {
  return (
    <footer className={style.footer}>
      <p className={style.texto}>Bolsa Família — consulta informativa</p>
    </footer>
  );
}

export default Footer;
