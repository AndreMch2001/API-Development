# Lista paginada (Spring): pagina=0..N, tamanho=20
REACT_APP_SEARCH_API=http://localhost:8080/api/Bolsafamiliamodel/todos
# Opcional: nome do parâmetro de filtro no backend (só enviado se o campo de busca tiver texto)
REACT_APP_SEARCH_QUERY_PARAM=nomeFavorecido
# Tamanho da página (igual ao da API)
REACT_APP_PAGE_SIZE=20
